// rfhutil.h — stub for unit test project.
// Test .cpp files in tests/UnitTests/ that do #include "rfhutil.h" land here
// (their directory is searched first). All content lives in prelude.h which is
// also force-included via /FI so source files from RFHUtil/ get the same stub.
#pragma once
#include "prelude.h"
